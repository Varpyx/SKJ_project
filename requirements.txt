# requirements.txt – Závislosti projektu
#
# Instalace:
#   pip install -r requirements.txt
#
# Spuštění serveru:
#   uvicorn main:app --reload --port 8000

# ---------------------------------------------------------------------------
# Jádro webového frameworku
# ---------------------------------------------------------------------------
fastapi==0.115.0          # Moderní, rychlý Python web framework pro API
uvicorn[standard]==0.30.0 # ASGI server pro spuštění FastAPI aplikace

# ---------------------------------------------------------------------------
# Zpracování souborů
# ---------------------------------------------------------------------------
python-multipart==0.0.9   # Potřebné pro parsování multipart/form-data uploadů
aiofiles==23.2.1           # Asynchronní čtení/zápis souborů (neblokuje event loop)

# ---------------------------------------------------------------------------
# Databáze
# ---------------------------------------------------------------------------
sqlalchemy>=2.0.35        # ORM – objektový přístup k relační databázi

# Poznámka: SQLite je součástí standardní Python knihovny,
# není třeba instalovat extra driver.
# Pro PostgreSQL bychom přidali: psycopg2-binary==2.9.9
alembic>=1.13.0   # Nástroj pro databázové migrace
